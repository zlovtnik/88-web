export const environment = {
  production: true,
  apiUrl: '/api',
  auth0: {
    domain: 'YOUR_AUTH0_DOMAIN',
    clientId: 'YOUR_AUTH0_CLIENT_ID',
    audience: 'YOUR_AUTH0_AUDIENCE' // Optional, if using API authorization
  }
}; 